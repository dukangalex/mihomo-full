# =============================================================================
# PUBLIC TEMPLATE — placeholders only. Do NOT commit real subscription URLs.
# On your VPS: let install.sh write this file, then run generate.sh.
# Public endpoint paths are generated once per installation and then kept stable.
# They intentionally use opaque natural-language words, not hex/UUID/token syntax.
# =============================================================================

# Airport Clash/Mihomo subscription URL (chain ENTRY). Replace the placeholder.
AIRPORT_SUB_URL="REPLACE_WITH_YOUR_AIRPORT_SUBSCRIPTION_URL"

# Public subscription paths. install.sh replaces these with unique generated paths.
# Do not reuse another installation's paths.
FIXED_FULL_CONFIG_PATH="/assets/REPLACE_WITH_RANDOM_WORD_PATH"
FIXED_EXIT_NODES_PATH="/assets/REPLACE_WITH_RANDOM_WORD_PATH"

# Domain already pointing to THIS VPS with HTTPS (no https://, no path).
DOMAIN="example.com"

# v2ray-agent local clashMeta directory
V2RAY_AGENT_CLASHMETA_DIR="/etc/v2ray-agent/subscribe_local/clashMeta"

# Generated files directory
OUTPUT_DIR="/opt/mihomo-full/output"
